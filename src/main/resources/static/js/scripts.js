$(document).ready(function() {
	
	/*
	$('#registerForm').on('submit', function() {
		event.preventDefault();
		 $.ajax({
	            type: 'POST',
	            url: '/register',
	            data: $(this).serializeArray(),
	            success: function(data) {
	                if (data == 'ok') {
	                    var p = $('<p>Felicidades, su cuenta ha sido creada!</p>')
	                    p.css({
	                    	"color":"green",
	                    	"font-size":"12px"
	                    })
	                    $('#registerForm').after(p)
	                    
	                    setTimeout(function() {
	                    	 location.href = "/inicio";
	                    }, 2000);
	                    
	                } else {
	                	 var p = $('<p>Por favor, corrija los campos erróneos</p>')
		                    p.css({
		                    	"color":"red",
		                    	"font-size":"12px"
		                    })
		                 $('#registerForm').after(p)
	                }
	            }
	        })
	})
	*/
	
	
	
	

})